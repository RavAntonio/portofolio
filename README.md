# Antonio Ravelonarivo — Portfolio

Portfolio Astro statique, prêt pour GitHub Pages.

## Commandes

```bash
npm install
npm run dev
npm run build
```

Le site utilise `base: '/portfolio'` pour un dépôt GitHub nommé `portfolio`. Si le dépôt porte un autre nom, adapte `astro.config.mjs`.
